package com.lawyeraid.gui;

public class MainWindow {

/*

		btnCalculeazaTermen.addActionListener(new ActionListener() {
			public void actionPerformed(ActionEvent arg0) {
				try {
					Date selectedDate = (Date) datePicker.getModel().getValue();
					
					if (selectedDate == null) {
						selectedDate = new Date();
					}
					
					TermModel term = (TermModel) tipTermenComboBox.getSelectedItem();
					
					int nrOfDays;
					
					if (term.getTermDuration() == termService.CUSTOM_TERM) {
						nrOfDays = Integer.parseInt(nrOfDaysTextField.getText());
					} else {
						nrOfDays = term.getTermDuration();
					}
					
					termDateTextField.setText(termService.getTermDate(selectedDate, nrOfDays));
				} catch (ParseException e) {
					errorLabel.setText(e.getMessage());
					return;
				} catch (NumberFormatException e) {
					errorLabel.setText("Verificati daca ati specificat corect numarul de zile.");
					return;
				} catch (Exception e) {
					errorLabel.setText("Oops, ceva nu-i bine!" + e.getMessage());
					return;
				}
				
				errorLabel.setText("");
			}
		});
	}
*/

}
